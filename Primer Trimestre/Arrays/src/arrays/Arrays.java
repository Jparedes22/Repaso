/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package arrays;

/**
 *
 * @author Usuario DAM 1
 */
public class Arrays {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        /**
         * int cont = 0; 
         * int[] numeros = {3434, 45345, 756, 1235457, 2354573,
         * 34673, 46573 ,23667543,234576, 325, 456357, 3235457, 32, 363467,
         * 63365, 3456}; //logitud = 9 //ultima posicion = longitud-1 String[]
         * palabras = new String[3];
         *
         * //acceso de lectura int valor = numeros[0];
         * System.out.println(valor); for (int i = 0; i < numeros.length; i++) {
         * System.out.println(numeros[i]); } for (int item : numeros) { if (item
         * % 2 == 0) { cont++; } } System.out.printf("Hay %d múltiplos de 2%n",
         * cont);
         */
        /* String[] palabras=new String[]{"fghtr","jlksdgh","pjsdfh"};
        for (String palabra : palabras) {
            System.out.printf("La palabra es %s%n",palabra);
        }
        boolean[]condiciones={true,false,true};
        char[]letras={'a','b','c'};
        double[]decimales={12.12,56.7};
         */
      /**  int contSt = 0;
        int contInt = 0;
        int contBoo = 0;
        int contDou = 0;
        int contCha = 0;
        Object[] cosas = {"palabra", 1, false, 4.50, 6.75, 7.45, 674.4, "regergeg", "keng", "ijrefg",
            123234, 234234, 345345, 45678, 2455632, true, false, true, 'a', 'g', 'n'};
        for (Object cosa : cosas) {
            if (cosa.getClass() == String.class) {
                contSt++;
            } else if (cosa.getClass() == Character.class) {
                contCha++;
            } else if (cosa.getClass() == Boolean.class) {
                contBoo++;
            } else if (cosa.getClass() == Integer.class) {
                contInt++;
            } else if (cosa.getClass() == Double.class) {
                contDou++;
            }
        }
        System.out.printf("%d palabras, %d caracteres, %d booleanos, "
                + "%d enteros, %d decimales%n", contSt, contCha, contBoo, contInt, contDou);
                * */
      
      
    }

}
